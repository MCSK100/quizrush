# --- AI questions via backend (recommended) ---
# 1. Deploy server/ to Render (render.yaml included) with GEMINI_API_KEY set there.
# 2. Paste your server URL below and rebuild the frontend.
VITE_AI_ENDPOINT=

# Example (local dev):
# VITE_AI_ENDPOINT=http://localhost:8787
