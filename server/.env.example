# 1. Get a free key at https://aistudio.google.com/apikey
# 2. Copy this file to `.env` and paste the key (server/.env — never committed)
# 3. Run: npm start   (or: node index.js)
GEMINI_API_KEY=

# Optional
PORT=8787
# GEMINI_MODEL=gemini-2.0-flash
